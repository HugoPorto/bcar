import { BudgetUpgradeStatements } from './budget.upgrade.statements';

describe('BudgetUpgradeStatements', () => {
  it('should create an instance', () => {
    expect(new BudgetUpgradeStatements()).toBeTruthy();
  });
});
